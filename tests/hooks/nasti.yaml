---
hooks:
  before_script: "tests/hooks/before_script.sh"
  after_script: "tests/hooks/after_script.sh"
  auto_cleanup: true
mutations:
  - name: "example_mutation"
    prompt: "Example Mutation"
    help: "Help for an example mutation"
    replace: "input_from_user"
    default: "{{ app_name }}"
    files: ["nasti.yaml"]
    validation:
      kind: "slug"